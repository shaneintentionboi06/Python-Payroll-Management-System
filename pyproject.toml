[project]
name = "Payroll-manager"
version = "0.1.0"
description = "This is supposed to a be my college project"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mysql-connector-python>=9.4.0",
    "textual>=6.7.1",
]
